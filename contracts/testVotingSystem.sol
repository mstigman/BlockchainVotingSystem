pragma solidity >= 0.4.24;

//import "./contracts/VotingSytem.sol";

// contract testVotingSystem {

//     function failCommitteeMember(address notOwner, address committee) public returns (bool success) {
//         //VotingSytem votingSystem = new VotingSytem();   // pass in as argument
//         //ThrowProxy throwproxy = new ThrowProxy(address(notOwner));  // pass in as argument
//         //VotingSytem(address(throwproxy)).addCommittee(committee);
//         //bool r = throwproxy.execute.gas(200000)();
//         bool r = false;
//         return r;
//     }

// }
